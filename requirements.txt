streamlit
pandas
streamlit-authenticator>=0.3.2
bcrypt>=4.0.1
PyYAML>=6.0
grep -qi '^PyYAML' requirements.txt || echo 'PyYAML>=6.0' >> requirements.txt
grep -qi '^streamlit-authenticator' requirements.txt || echo 'streamlit-authenticator>=0.3.2' >> requirements.txt
grep -qi '^bcrypt' requirements.txt || echo 'bcrypt>=4.0.1' >> requirements.txt